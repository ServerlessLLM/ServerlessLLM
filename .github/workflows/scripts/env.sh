sudo    apt-get update
sudo    apt-get install -y --no-install-recommends \
        software-properties-common

sudo    apt-get install -y wget bzip2 ca-certificates git \

sudo    apt-get clean
sudo    rm -rf /var/lib/apt/lists/*